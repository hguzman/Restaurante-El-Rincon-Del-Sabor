<%= simple_form_for [@category, @dish] do |f| %>
 <%= f.input :nombre %>
 <%= f.input :existencia %>
 <%= f.input :precio %>
 <%= f.input :descripcion %>

 <%= f.submit @dish.new_record? ? "Crear Plato" : "Actualizar Plato", :class => "btn btn-block btn-primary"%>

<% end %>
